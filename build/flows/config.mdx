---
title: 'Flow config and callable interface'
description: 'Metadata, entrypoints, and the contract exposed by a flow'
---

The flow config surfaces answer a different question than the editor:

> What does this flow expose to callers and runtime bindings?

## Core concerns

Use the config-oriented pages for:

- metadata
- interface shape
- schema-backed input and output expectations
- entrypoint and trigger-facing details
